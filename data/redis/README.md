# Redis

Redis used as a cache / ephemeral datastore.

## Persistence

- Append Only File (AOF) enabled
- Data stored in `redis_data` volume

## Security

- Protected mode enabled
- No external exposure required
